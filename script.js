const wedding=new Date("2027-09-18T15:00:00+02:00");function tick(){const d=Math.max(0,wedding-new Date());days.textContent=Math.floor(d/86400000);hours.textContent=Math.floor(d/3600000)%24;minutes.textContent=Math.floor(d/60000)%60;seconds.textContent=Math.floor(d/1000)%60}tick();setInterval(tick,1000);const nav=document.getElementById("nav"),progress=document.getElementById("progress");addEventListener("scroll",()=>{nav.classList.toggle("scrolled",scrollY>60);const h=document.documentElement.scrollHeight-innerHeight;progress.style.width=(h?scrollY/h*100:0)+"%"});const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add("visible")}),{threshold:.12});document.querySelectorAll(".reveal").forEach(e=>obs.observe(e));menu.onclick=()=>links.classList.toggle("open");links.querySelectorAll("a").forEach(a=>a.onclick=()=>links.classList.remove("open"));rsvpForm.addEventListener("submit",e=>{e.preventDefault();const s=encodeURIComponent(`RSVP trouwweekend - ${name.value}`);const b=encodeURIComponent(`Naam: ${name.value}
E-mailadres: ${email.value}
Aanwezig: ${attendance.value}
Partner: ${partner.value||"-"}
Dieetwensen/opmerkingen: ${message.value||"-"}`);location.href=`mailto:fbcdevree@gmail.com,ambervanderhorst@hotmail.com?subject=${s}&body=${b}`});